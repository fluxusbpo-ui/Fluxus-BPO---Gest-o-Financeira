// Deprecated Knex seed for planos. Seeds are handled via scripts/seed_plans.js using Supabase.
exports.seed = async function(knex) {
  console.warn('Deprecated seed: Run scripts/seed_plans.js to seed Supabase');
  return Promise.resolve();
};
